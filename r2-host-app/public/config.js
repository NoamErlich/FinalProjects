/**
 * Runtime environment config for the host app.
 * The web component reads the same window.__env__ object.
 */
window.__env__ = {
  BACKEND_URL: 'https://api.example.com',
  THEME_MODE: 'dark',
  APP_NAME: 'R2 Dashboard',
};
